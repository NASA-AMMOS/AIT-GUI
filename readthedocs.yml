build:
  image: latest
formats:
    - none
python:
    version: 3.7
    pip_install: true
    install:
      - requirements: doc/requirements.txt
    extra_requirements:
        - docs
        - tests
